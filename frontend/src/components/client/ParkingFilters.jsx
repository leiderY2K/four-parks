const ParkingFilters = () => {
  return (
    <section>
        <select id="parking-city" className="bg-white">
            <option value=""> Ciudad </option>
        </select>
    </section>
  )
}

export default ParkingFilters